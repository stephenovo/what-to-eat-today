# “今天吃什么”产品梳理

## 一句话定位

把“我家有什么”变成“今天能吃什么”的本地优先 iPhone App；保留噜噜冰箱亲切、轻松、不责备人的气质，但不再依赖 K3 或任何硬件。

## 核心用户闭环

```text
手动记录食材 → 本机保存库存 → 计算菜谱匹配度
      ↑                              ↓
吃掉后减库存 ← 查看步骤 / 外部视频 ← 选择一道菜
                                      ↓
                              缺料则跳转零售平台
```

## 第一版原则

1. **先有一个可靠闭环。** 库存、推荐和跳转真实可用，拍照识别不做假的演示。
2. **本地即产品。** 不要求账号、配对、服务器或网络；数据只在当前手机。
3. **模型只提建议。** 将来接入拍照后，识别结果必须经用户确认才能写入库存。
4. **官方渠道完成后续动作。** App 只构造搜索词并跳转，不保存美团、HKTVmall、抖音或小红书账号。
5. **临期优先。** 相同匹配度下，能用掉临期食材的菜谱排序更靠前。

## 已实现范围

| 模块 | 第一版能力 |
|---|---|
| 冰箱 | 30 个常用食材、自定义名称、数量、单位、保质期、本地持久化、增减与删除 |
| 推荐 | 12 道内置家常菜、匹配率、缺料、直接可做、临期加权、按时间二次排序 |
| 菜谱 | 2 人份食材、步骤、技巧、当前库存核对 |
| 学做菜 | 抖音、小红书搜索入口，App 不可用时回退网页 |
| 买缺料 | 美团、HKTVmall 搜索入口；一次携带全部缺料关键词 |
| 拍照 | UI 入口与 `IngredientRecognitionService` 协议已预留，识别暂未实现 |

## 后续路线

- **1.1 拍照识别：** 单物体拍照 → 候选名称/置信度 → 用户确认 → 写入库存。
- **1.2 个性化：** 忌口与过敏硬过滤、口味偏好、人数和历史多样性。
- **1.3 库存消耗：** 做完一道菜后按食材逐项确认扣减，保留可撤销记录。
- **1.4 菜谱扩展：** 支持用户收藏、导入链接，以及可更新的签名菜谱包。
- **1.5 可选同步：** 仅在用户明确需要家庭共享时增加 iCloud，同步不是使用前提。
